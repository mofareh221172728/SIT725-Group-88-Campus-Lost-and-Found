# Test environment template — copy this to .env.test and fill in your values.
# .env.test is gitignored to protect local credentials.
#
# IMPORTANT: MONGODB_URI must point to a dedicated TEST database.
# Never use the development or production database URI here.
# The test runner will DROP this database after each test run.

NODE_ENV=test
PORT=3001
MONGODB_URI=mongodb://localhost:27017/sit725-group-88-test
